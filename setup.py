from setuptools import setup, find_packages

setup(
    name="veeam-spc",
    version="0.1.0",
    description="Barebones REST API wrapper for Veeam Service Provider Console",
    author="Cenvora",
    packages=find_packages(),
    install_requires=[],
    python_requires=">=3.7",
)
